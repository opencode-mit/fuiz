{
  "rewrites": [{ "source": "/dist/server/(.*)", "destination": "/api" }],
  "builds": [
      {
          "src": "/api/index.js",
          "use": "@vercel/node"
      }
  ],
  "routes": [
      {
        "src": "/src/server/(.*)",
        "dest": "/api/index.js"
      }
  ]
}